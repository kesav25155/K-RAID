kind = "web"
previewPath = "/"
title = "K-RAID"
version = "1.0.0"
id = "artifacts/k-raid"
router = "path"

[[integratedSkills]]
name = "react-vite"
version = "1.0.0"

[[services]]
name = "web"
paths = [ "/" ]
localPort = 5173

[services.development]
run = "pnpm --filter @workspace/k-raid run dev"

[services.production]
build = [ "pnpm", "--filter", "@workspace/k-raid", "run", "build" ]
publicDir = "artifacts/k-raid/dist/public"
serve = "static"

[[services.production.rewrites]]
from = "/*"
to = "/index.html"

[services.env]
PORT = "5173"
BASE_PATH = "/"
